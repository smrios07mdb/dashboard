# Chunk 33 smoke — 2026-07-30 ~15:45 ET

| # | Check | Result | Note |
|---|-------|--------|------|
| 1 | Anchorless picker | PASS | ⋯ → "Set priority…" opened the popover directly adjacent (below) the chip-less "Smoke test A" row, not at a corner; "P2 — Soon" applied an amber-tinted P2 chip (bg srgb .85/.54/.11 @16%). Screenshot taken. |
| 2 | Chip/picker/edge  | PASS | Chip click reopened picker (checkmark on current); "P1 — Urgent" → red chip rgba(184,70,46,.13) + 3px solid rgb(184,69,46) left border on row; "Clear priority" removed chip and border returned to transparent. |
| 3 | Realtime          | PASS | Tab 1 P3→P1 appeared in tab 2 in **1.46s** (click ts 1785428446336 → MutationObserver ts 1785428447791), no reload (observer + window state survived). Bonus: iframe→parent instance also synced during check 5. |
| 4 | Sort + persist    | PASS* | Priority: PASS — P1 > P2 > P3 > 4 no-priority rows (DOM-verified in TicketPlus, 8 open). Estimate: PASS — 10m row first, 30m rows after (created "Smoke test B" @10m since every real task is 30m — otherwise unassessable). Due date: **not assessable** — no due dates visible on any rows. Persistence: PASS — control read "Sort · Estimate" after reload; restored to Priority. *Observation: sort control is global — changing Work's sort also changed Personal's label. |
| 5 | Narrow viewport   | PASS* | *Method caveat: `resize_window` was a no-op (Chrome window pinned at 1092×1100, likely macOS fullscreen), so tested in a same-origin 390×844 iframe, which drives real viewport/media-query layout. Mobile layout rendered (bottom tab bar); chip↔minutes gap 12px, no overlap (rect-verified); chip tap opened picker fully in-viewport; "Soon" applied P2 correctly. Screenshot taken. |
| 6 | Console           | PASS* | Zero errors. *Caveat: extension console tracking only starts on first read, mid-session — so the page was reloaded and every path re-exercised under tracking (load, both picker entry points, set/clear priority, sort switches, deletes + confirm modals, plus the narrow-viewport iframe instance). No errors captured. |

Tasks touched: "Smoke test A" (created, cycled P2/P1/clear/P3/P1/P2, deleted). "Smoke test B" (created @10m estimate for the estimate-sort check, cycled P1/clear, deleted). Real tasks: "Call Artem" (none→P2→cleared back to none), "Ask Roth who passed" (none→P3→cleared back to none) — both had no priority originally; fully restored. No estimates, titles, or other fields changed on real tasks. Final state verified: TicketPlus 6 open / Work 17 open / 8h 30m, no chips anywhere, sort = Priority.
