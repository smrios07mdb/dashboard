# Chunk 35 smoke — 2026-07-30 ~14:25 ET

Run by Cowork (Chrome MCP) against local dev server `http://localhost:5173/dashboard/`, `redesign` @ `5cfa74e` (includes ef9e04f), clean tree. Dev DB `dctfspcbkqvvyptddtif`, single user `9b5ce57c-8b69-42b5-ba0c-d3e21b269f85`.

| # | Check | Result | Note |
|---|-------|--------|------|
| 1 | Bad-URL 422 path | PASS | `https://example.com` → button cycled to "Verifying…" and re-enabled; error toast **"That URL isn't an iCalendar feed."** (the `invalid_feed` mapping — the correct one for this input; not the `invalid_url` pre-check message, not `unreachable`). Badge stayed **Not connected**; after reload still Not connected; DB confirmed `outlook_status='unconfigured'`, no URL persisted. |
| 2 | Happy path (public ICS) | PASS | Google US-holidays ICS → toast **"Connected — Holidays in United States · 0 events this week"** (N=0, allowed — sparse holiday feed). Status row flipped to green **Connected · Holidays in United States** + "Last refreshed just now" line. Input cleared to placeholder. Disconnect button appeared under ICS row. |
| 3 | Write-only URL | PASS | After reload: ICS input empty (never prefilled); JS-verified `document.body.innerHTML.includes('basic.ics') === false` (also checked `calendar.google.com` — absent). Badge still Connected (read back from settings). |
| 4 | Realtime badge flip | PASS | SQL `outlook_status='unreachable'` (via Supabase MCP) → **without reload**, amber warning badge **"Feed unreachable since 14:20"** (24h clock of stored `outlook_fetched_at`) + cached-data line "Showing busy times cached at 14:20. The feed is retried on the next busy refresh." Amber/warning variant, not destructive red (screenshot). Observed within ≤3s (checked after a 3s wait). Restore `outlook_status='ok'` → green **Connected · Holidays in United States** returned, still no reload. |
| 5 | Disconnect | PASS | Confirm dialog **"Disconnect Outlook feed?"** — copy: "This removes the stored feed link from this app. Busy times from Outlook disappear; the published calendar in Outlook is untouched." Confirm → toast **"Outlook feed disconnected."**; badge → **Not connected**; Disconnect button gone. DB: `outlook_status='unconfigured'`, `outlook_ics_url_encrypted is null`. |
| 6 | Console + Apple untouched | PASS* | Reloaded with console tracking cleared, re-exercised checks 1, 2, 5 (bad URL → error toast; reconnect → Connected toast/badge; disconnect → dialog + toast + Not connected). *One console entry at ERROR level: `Calendar action failed CalendarError: That URL isn't an iCalendar feed.` (Settings.tsx catch → `console.error`, fired by the **deliberate** check-1 bad-URL re-exercise — the app's designed logging of the induced failure, not an unexpected error; zero other errors/exceptions across all flows). Apple rows unchanged throughout: green "Connected · verified …" status badge, Apple ID `srios@smu.edu`, app-password placeholder, Test connection / Save / Disconnect — all rendering as before, section titled "Calendars". |

End state verified (DB + UI): Outlook feed **disconnected** — `outlook_status='unconfigured'`, `outlook_ics_url_encrypted` null, `outlook_feed_name` null, `outlook_fetched_at` null; badge Not connected. Apple Calendar connection left exactly as found (Connected, `srios@smu.edu`). No other settings touched.

Harness notes: the Chrome-MCP `type` action twice failed silently to enter text into the ICS input after a fresh navigation (input stayed empty — caught by JS value checks before clicking Verify); resolved by re-clicking + retyping once and by `form_input` for the check-6 re-exercise. Harness quirk only — no app behavior involved. Per spec, screenshots are inline in the Cowork transcript only.
